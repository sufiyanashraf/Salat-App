---
wave: 1
depends_on: []
files_modified:
  - app/gradle/libs.versions.toml
  - app/app/build.gradle.kts
  - app/app/src/main/AndroidManifest.xml
  - app/app/src/main/assets/efficientnet_lite0.tflite
  - app/app/src/main/java/com/example/salattracker/ml/ImageClassifierHelper.kt
  - app/app/src/main/java/com/example/salattracker/ui/main/MainScreen.kt
  - app/app/src/main/java/com/example/salattracker/ui/lock/LockScreenOverlay.kt
  - app/app/src/main/java/com/example/salattracker/service/AppLockAccessibilityService.kt
autonomous: true
---

# Phase 04: ML & Verification Plan

## Verification Criteria
- [ ] `./gradlew assembleDebug` succeeds.
- [ ] `app/app/src/main/java/com/example/salattracker/ml/ImageClassifierHelper.kt` exists.
- [ ] `app/app/src/main/assets/efficientnet_lite0.tflite` (Model Asset) exists.
- [ ] `LockScreenOverlay.kt` contains a "Verify with Camera" button.

## Requirements Covered
- VERI-01
- VERI-02
- VERI-03

## must_haves
- CameraX and MediaPipe dependencies must be added to `libs.versions.toml` and `build.gradle.kts`.
- Camera permissions (`android.permission.CAMERA`) and feature must be in `AndroidManifest.xml`.
- A generic model (e.g. `efficientnet_lite0.tflite`) must be downloaded to `assets` directory.
- `ImageClassifierHelper` class must be created utilizing MediaPipe.
- `LockScreenOverlay` must be updated to host CameraX preview and `ImageCapture` usecase.
- `LockManager.setLocked(false)` must be called upon successful verification.

## Tasks

<tasks>
  <task>
    <id>4-01-01</id>
    <description>Add CameraX, MediaPipe dependencies and Camera permissions</description>
    <read_first>
      - app/gradle/libs.versions.toml
      - app/app/build.gradle.kts
      - app/app/src/main/AndroidManifest.xml
    </read_first>
    <action>
      - Open `app/gradle/libs.versions.toml` and add `camerax = "1.6.1"` and `mediapipe = "0.10.35"` to `[versions]`.
      - In `[libraries]` of `app/gradle/libs.versions.toml`, add:
        `androidx-camera-core = { module = "androidx.camera:camera-core", version.ref = "camerax" }`
        `androidx-camera-camera2 = { module = "androidx.camera:camera-camera2", version.ref = "camerax" }`
        `androidx-camera-lifecycle = { module = "androidx.camera:camera-lifecycle", version.ref = "camerax" }`
        `androidx-camera-view = { module = "androidx.camera:camera-view", version.ref = "camerax" }`
        `mediapipe-tasks-vision = { module = "com.google.mediapipe:tasks-vision", version.ref = "mediapipe" }`
      - Open `app/app/build.gradle.kts` and add the `implementation` dependencies for all the above libraries inside the `dependencies` block.
      - Open `app/app/src/main/AndroidManifest.xml` and add `<uses-permission android:name="android.permission.CAMERA" />` and `<uses-feature android:name="android.hardware.camera" android:required="false" />` inside the `<manifest>` tag before `<application>`.
    </action>
    <acceptance_criteria>
      - `app/gradle/libs.versions.toml` contains `camerax = "1.6.1"`
      - `app/app/build.gradle.kts` contains `implementation(libs.androidx.camera.core)`
      - `app/app/src/main/AndroidManifest.xml` contains `<uses-permission android:name="android.permission.CAMERA" />`
    </acceptance_criteria>
  </task>

  <task>
    <id>4-01-02</id>
    <description>Download MediaPipe model and create ImageClassifierHelper</description>
    <read_first>
      - .planning/phases/04-ml-verification/04-RESEARCH.md
    </read_first>
    <action>
      - Create the directory `app/app/src/main/assets` if it does not exist.
      - Use PowerShell or a custom script to download `https://storage.googleapis.com/mediapipe-tasks/image_classifier/efficientnet_lite0_uint8.tflite` to `app/app/src/main/assets/efficientnet_lite0.tflite`.
      - Create `app/app/src/main/java/com/example/salattracker/ml/ImageClassifierHelper.kt`.
      - Implement `class ImageClassifierHelper(private val context: Context)` exactly as detailed in `04-RESEARCH.md` using `com.google.mediapipe.tasks.vision.imageclassifier.ImageClassifier`.
      - Set the model asset path to `efficientnet_lite0.tflite` via `BaseOptions.builder().setModelAssetPath("efficientnet_lite0.tflite").build()`.
      - Ensure `classify` returns `true` if any top result matches `listOf("rug", "doormat", "fabric", "velvet", "window shade")` with a score > `0.5f`.
    </action>
    <acceptance_criteria>
      - `app/app/src/main/assets/efficientnet_lite0.tflite` exists
      - `app/app/src/main/java/com/example/salattracker/ml/ImageClassifierHelper.kt` contains `class ImageClassifierHelper`
      - `app/app/src/main/java/com/example/salattracker/ml/ImageClassifierHelper.kt` contains `efficientnet_lite0.tflite`
    </acceptance_criteria>
  </task>

  <task>
    <id>4-01-03</id>
    <description>Request Camera Permission in MainScreen</description>
    <read_first>
      - app/app/src/main/java/com/example/salattracker/ui/main/MainScreen.kt
    </read_first>
    <action>
      - Open `app/app/src/main/java/com/example/salattracker/ui/main/MainScreen.kt`.
      - Use `rememberLauncherForActivityResult` with `ActivityResultContracts.RequestPermission()` to request `android.permission.CAMERA`.
      - Launch the permission request when the screen is composed using `LaunchedEffect(Unit)`.
      - Before launching, check `ContextCompat.checkSelfPermission(context, android.Manifest.permission.CAMERA) != PackageManager.PERMISSION_GRANTED`.
    </action>
    <acceptance_criteria>
      - `app/app/src/main/java/com/example/salattracker/ui/main/MainScreen.kt` contains `ActivityResultContracts.RequestPermission()`
      - `app/app/src/main/java/com/example/salattracker/ui/main/MainScreen.kt` contains `android.Manifest.permission.CAMERA`
    </acceptance_criteria>
  </task>

  <task>
    <id>4-01-04</id>
    <description>Integrate Camera UI into LockScreenOverlay</description>
    <read_first>
      - app/app/src/main/java/com/example/salattracker/ui/lock/LockScreenOverlay.kt
      - .planning/phases/04-ml-verification/04-RESEARCH.md
    </read_first>
    <action>
      - In `LockScreenOverlay.kt`, add a new callback parameter `onVerifySuccess: () -> Unit`.
      - Add a state variable `var showCamera by remember { mutableStateOf(false) }`.
      - When `showCamera` is false, display the existing lock screen UI and add a "Verify with Camera" `Button` below the main message. Clicking it sets `showCamera = true`.
      - When `showCamera` is true, first check if `android.Manifest.permission.CAMERA` is granted using `ContextCompat.checkSelfPermission(LocalContext.current, android.Manifest.permission.CAMERA)`.
      - If permission is granted, display a full-screen camera UI using `AndroidView(factory = { androidx.camera.view.PreviewView(it) })`.
      - If permission is denied, display a fallback UI (e.g. a `Text` saying "Camera Permission Required") to prevent a `SecurityException` and service crash.
      - Initialize `ProcessCameraProvider` and bind `Preview` and `ImageCapture` use cases to `androidx.lifecycle.compose.LocalLifecycleOwner.current`.
      - Initialize `ImageClassifierHelper(context)`.
      - Add a "Capture" `Button` layered over the `PreviewView`. On click, launch a coroutine to capture the image using the `captureAndVerify` logic from `04-RESEARCH.md`.
      - Ensure `ImageProxy.toBitmap()` is used, `rotationDegrees` is passed, `classifierHelper.classify()` is called, and crucially `imageProxy.close()` is in the `finally` block.
      - If verification succeeds, call `onVerifySuccess()`. If false, show a Toast "Mat not detected" and keep the camera open.
      - Add a "Cancel" button to close the camera (`showCamera = false`).
    </action>
    <acceptance_criteria>
      - `app/app/src/main/java/com/example/salattracker/ui/lock/LockScreenOverlay.kt` contains `AndroidView(factory`
      - `app/app/src/main/java/com/example/salattracker/ui/lock/LockScreenOverlay.kt` contains `Verify with Camera`
      - `app/app/src/main/java/com/example/salattracker/ui/lock/LockScreenOverlay.kt` contains `imageProxy.close()`
    </acceptance_criteria>
  </task>

  <task>
    <id>4-01-05</id>
    <description>Wire verification success to unlock phone and verify build</description>
    <read_first>
      - app/app/src/main/java/com/example/salattracker/service/AppLockAccessibilityService.kt
      - app/app/src/main/java/com/example/salattracker/lock/LockManager.kt
    </read_first>
    <action>
      - In `AppLockAccessibilityService.kt`, update the `overlayParams` to include `WindowManager.LayoutParams.FLAG_SHOW_WHEN_LOCKED` using the bitwise `or` operator.
      - In `AppLockAccessibilityService.kt`, update the `LockScreenOverlay` instantiation within `initOverlayView()`.
      - Pass `onVerifySuccess = { LockManager.setLocked(false) }` to `LockScreenOverlay`.
      - Ensure there are no unresolved references or imports in `AppLockAccessibilityService.kt`.
      - Build the app using `./gradlew assembleDebug` in the `app/` directory to ensure no compilation errors.
    </action>
    <acceptance_criteria>
      - `app/app/src/main/java/com/example/salattracker/service/AppLockAccessibilityService.kt` contains `FLAG_SHOW_WHEN_LOCKED`
      - `app/app/src/main/java/com/example/salattracker/service/AppLockAccessibilityService.kt` contains `onVerifySuccess = { LockManager.setLocked(false) }`
      - `cd app && ./gradlew assembleDebug` exits with 0
    </acceptance_criteria>
  </task>
</tasks>
